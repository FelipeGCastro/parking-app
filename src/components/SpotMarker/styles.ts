import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
  },
  textContainer: {
    paddingVertical: 4,
    paddingHorizontal: 8,
    backgroundColor: '#fff',
    // marginTop: 50,
    marginLeft: 50,
    borderRadius: 4,
  },
  textTime: {
    fontSize: 12,
  },
})
