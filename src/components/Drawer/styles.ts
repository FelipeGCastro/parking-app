import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  userContainer: {
    flexGrow: 1,
    alignSelf: 'center',
  },
  userPhoto: {
    width: 80,
    height: 80,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#707070',
    alignItems: 'center',
    justifyContent: 'center',
  },
})
