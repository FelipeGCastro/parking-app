import React from 'react'
import { View } from 'react-native'

export const iconNames = ['']

interface Props {
  name: string
}

const Icon = ({ name }: Props) => {
  return <View />
}

export default Icon
